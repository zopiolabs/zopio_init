# Docker Container Scan Trigger Test

This file is added to test the container security scan trigger condition.
The commit message will contain the word "docker" which should trigger the container scan job.